should be able to link multiple with one connection:

connect instanceID$-connectionname(destination$)
destination$ = destination$(i) + destination$(i + 1) for multiple destinations


creating new point:

new instancename$ | calls new point menu (why did i build the menu library otherwise? :p)

new instancename$(parameters$) | creates a new point within a simple command, good for quick use and adding simple points
parameters$ = "c=" + category$ + "f=" + file$ + "v=" + value$